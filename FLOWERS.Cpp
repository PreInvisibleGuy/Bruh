#include <bits/stdc++.h>
#define N 100001
using namespace std;
int n,kq,f[N];
long long a[N];
int main()
{
     ios::sync_with_stdio(0);
    cin.tie(0);
    cout.tie(0);
    freopen("FLOWERS.INP","r",stdin);
    freopen("FLOWERS.OUT","w",stdout);
    cin>>n;
    for(int i=1;i<=n;i++)
        cin>>a[i];
    f[1]=1;f[2]=2;
    for(int i=3;i<=n;i++)
    {
        if()
    }
}
