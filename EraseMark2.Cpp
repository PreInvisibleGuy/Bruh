#include<bits/stdc++.>
using namespace std;
int main()
{
    long long m,k,a[10001];
    cin>>m>>k;
    long long n=2*m;
    for(int i=1;i<=n;i++)
        cin>>a[i];

    for(int i=m-k;i<=n-2*k;i++)
    {
        if(a[i]==a[])
    }
}
